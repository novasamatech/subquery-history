{
  "name": "subquery-nova-altair",
  "version": "1.0.0",
  "description": "Project for getting operation history fo Altair",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prebuild": "scp -r ../../src ./ & scp ../../tsconfig.json ./",
    "prepack": "rm -rf dist && npm build",
    "codegen": "npx subql codegen",
    "validate": "./node_modules/.bin/subql validate"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/cli": "^0.19.0",
    "@subql/types": "^0.13.0",
    "typescript": "^4.5.5"
  }
}
